<div class="motion-backdrop" aria-hidden="true">
  <div class="flow-field"></div>
  <div class="flow-lines"></div>
</div>

<style>
  .motion-backdrop {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .flow-field,
  .flow-lines {
    position: absolute;
    inset: -18%;
  }

  .flow-field {
    background:
      linear-gradient(118deg, transparent 6%, rgba(16, 185, 129, 0.13) 28%, transparent 48%),
      linear-gradient(74deg, transparent 12%, rgba(125, 211, 252, 0.12) 39%, transparent 58%),
      linear-gradient(154deg, transparent 18%, rgba(251, 191, 36, 0.12) 52%, transparent 74%);
    filter: blur(34px);
    opacity: 0.86;
    transform: rotate(-7deg) translate3d(0, 0, 0);
    animation: flow-drift 18s ease-in-out infinite alternate;
  }

  .flow-lines {
    background-image:
      linear-gradient(90deg, rgba(24, 24, 27, 0.035) 1px, transparent 1px),
      linear-gradient(0deg, rgba(24, 24, 27, 0.028) 1px, transparent 1px);
    background-position: center;
    background-size: 84px 84px;
    mask-image: radial-gradient(circle at 50% 20%, black 0%, transparent 68%);
    opacity: 0.55;
    transform: translate3d(0, 0, 0);
    animation: line-drift 28s linear infinite;
  }

  @keyframes flow-drift {
    0% {
      transform: rotate(-7deg) translate3d(-2.2rem, -1rem, 0) scale(1);
    }

    50% {
      transform: rotate(-3deg) translate3d(1.4rem, 2rem, 0) scale(1.04);
    }

    100% {
      transform: rotate(-9deg) translate3d(2.3rem, -0.8rem, 0) scale(1.02);
    }
  }

  @keyframes line-drift {
    from {
      background-position: 0 0;
    }

    to {
      background-position: 84px 84px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .flow-field,
    .flow-lines {
      animation: none;
    }
  }
</style>
